# CoinPilot AI · 币航 — AI 加密交易工作台

工作台是现有迷你窗口的扩展。默认仍显示原来的 28px 迷你行情窗口，双击迷你窗口即可打开工作台，也可右键选择“交易台”或双击托盘图标。工作台已打开时会唤回原窗口。关闭工作台只隐藏窗口；隐藏迷你窗口不停止监控；托盘或迷你窗口菜单中的“退出”会结束整个程序。

迷你窗口长按自由拖动，松开后保存位置；短按刷新行情。磁吸功能位于图表内，用于对齐 K 线高低点。

程序按当前 Windows 用户限制日常单实例，源码与 EXE 共用锁；再次启动会唤回已有工作台，`--settings` 则打开原有设置。即使传入另一个配置路径，也不会另开实例或切换已有配置；需要切换配置时先退出。异常退出后，下次启动会回收已死亡进程的锁。内部 `--quit-after` 仅用于隔离启动验收，不影响日常实例。

托盘单击恢复迷你窗口、双击打开工作台、右键显示操作菜单。绿色角标表示未读提醒，黄色角标表示暂停弹出通知，暂停时事件仍记录。工作台数据库启动失败时托盘和迷你窗口仍保留。Windows 可能把托盘图标放在任务栏的“显示隐藏的图标”区域。

应用标志使用 `coinpilot_ai/assets/app_icon/` 中提供的 16、32、64、128、256 像素图像，统一用于窗口、工作台、托盘及发布产物；托盘保留状态角标。功能图标以 SVG 随程序打包，无需登录或在线加载，采用 Lucide 的 ISC / Feather MIT 许可图标，许可证位于 `coinpilot_ai/assets/lucide/LICENSE`；趋势线、斐波拉契为本项目绘制。

```powershell
uv run --locked coinpilot-ai.py --workbench
```

## 可拆分工作区

“工作台、复盘、视图、设置”直接位于窗口标题栏，不再单独占用一行导航。标题按当前环境显示“CoinPilot AI · 本地模拟”“CoinPilot AI · 模拟环境”或“CoinPilot AI · 真实环境”。标题栏空白处可拖动窗口，双击最大化／还原，右侧按钮提供最小化、最大化／还原和关闭；关闭仍只隐藏窗口，后台服务继续运行。

原“盯盘”和“交易”合并为“工作台”。默认左侧为 AI 预留区，中间为盘面及底部账户信息，右侧为委托下单。复盘和设置继续保留独立入口。

拖动面板标题栏可改变停靠位置或拖出独立窗口，支持跨屏、调整大小及标签叠放。双击标题栏或点击浮动按钮可浮动／停靠；关闭按钮只隐藏该面板。标题栏的“视图”菜单可恢复隐藏面板；面板显示开关、锁定布局、恢复默认布局也统一放在“设置 → 工作区布局”。锁定限制停靠拖动，不禁止移动 Windows 浮动窗口。

盘面侧栏按钮展开自选列表，底栏按钮显示／隐藏账户与交易信息。“放大”临时隐藏其他面板，再次点击恢复。账户区按标签展示持仓、挂单、止盈止损、本机提交、成交、提醒和账户；平仓、撤单等按钮跟随对应标签显示。面板变窄时，标签自动收起为分类下拉框；表格保留可读列宽并提供横向滚动，可拖动表头调整列宽，悬停查看完整内容。

布局自动保存在原工作台数据库，与交易环境无关；首次升级使用新默认布局，不删除旧画线、视口、指标设置和布局记录。主窗口关闭时全部浮动面板一起隐藏，后台继续运行；重新打开恢复此前可见面板和未提交表单。切换复盘或设置时暂时隐藏浮动面板，返回工作台恢复。显示器拔除后会将窗口移回可用屏幕；布局损坏时恢复默认排列。

AI 面板暂时只显示“AI 功能规划中”，不挂载原交易计划或事件解读面板，不初始化请求或读取凭据。已有 AI 配置、报告、复盘页和按配置触发的后台分析仍保留，后续确定功能后再接入。

## 统一设置入口

所有设置集中在工作台“设置”页，左侧分类为常规与网络、图表、工作区布局、OKX 账户、AI 服务、提示词模板、提醒规则、桌面与通知、软件更新。迷你窗口、托盘及 `--settings` 均跳转到同一页，不再额外打开迷你窗口设置或 EMA 设置弹窗。

常规设置保存后生效，“撤销未保存修改”恢复已保存配置；EMA 同样保存后应用。页面切换、隐藏和恢复工作台保留草稿。磁吸、通知及工作区开关立即生效并保存；浮动面板在设置页期间隐藏，返回工作台时按新的布局设置恢复。

软件更新检查、下载进度和退出安装按钮直接显示在设置页。开始准备安装后禁用工作台交互，助手失败则恢复；仍须确认后退出，不自动执行交易或强制结束程序。原有设置路径、SQLite 与凭据存储不变。

## 配置与数据

- 迷你窗口继续读取原 JSON 配置，保留三源行情、轮播、快捷键、透明度、代理和开机启动设置。
- 工作台配置、提醒规则、订单快照、成交、费用、提示词版本和报告保存在配置文件旁的 `*.workbench.sqlite3`。默认是 `%USERPROFILE%\crypto_widget_settings.workbench.sqlite3`。
- OKX 与 AI 密钥保存在 Windows Credential Manager。凭据命名空间包含数据库路径的摘要；移动配置和数据库后需要重新保存凭据。没有明文密钥存储降级路径。
- `--config` 与 `--cache-dir` 仍有效。模拟、真实环境和不同 API Key 的账户记录分别存储；切换环境不会混用交易记录。
- 工作台使用“设置 → 常规与网络”中保存的代理；正常退出后可备份 SQLite 文件。程序运行时数据库还可能有 `-wal` 和 `-shm` 文件，不应只复制主文件。

## 盯盘与提醒

工作台行情、图表和提醒只使用 OKX USDT 永续。自选默认迁移原来的 BTC / ETH / SOL 配置，可以增删；持仓与启用规则涉及的合约也会订阅。迷你窗口依旧可以使用 Binance、OKX、Bybit，不把其报价当作 OKX 下单依据。

原生 Qt 图表采用 TradingView 风格布局，显示 K 线、成交量及自定义 EMA，支持 1m、5m、15m、1H、4H、1D。时间在界面显示为本地时间。工作台公共报价与 K 线均使用 OKX 生产公共行情源；模拟环境隔离账户和交易，不另外构造一套报价。

K 线通过 OKX 业务 WebSocket 实时更新，约 20 秒进行 REST 校准，WebSocket 断开时改为约 5 秒轮询。初次加载 300 根，向左浏览时分页加载更早历史；重启恢复较早视口时直接请求对应时间段。长时间断线造成的缺口会补齐，期间不会把缺口标成新鲜数据。历史是否可获取以 OKX 接口结果为准。账户约 8 秒刷新，成交与历史约 60 秒增量同步。

### 图表操作

| 操作 | 行为 |
| --- | --- |
| 空白处按住左键拖动 | 左右移动时间，上下移动价格；手动纵向移动后关闭自动价格范围 |
| 滚轮 | 以指针为中心缩放时间轴 |
| 拖动右侧价格轴 | 拉伸／压缩价格范围；双击恢复自动缩放 |
| 拖动底部时间轴 | 调整 K 线间距；双击恢复默认间距并返回最新 |
| 拖动主图与成交量之间的分隔线 | 调整成交量区域高度 |
| 顶部“自动”“最新” | 恢复自动价格范围，或恢复跟随最新蜡烛 |
| “放大”“底栏”“侧栏” | 放大图表、显示／隐藏账户信息、展开／收起自选列表 |
| 点击 EMA 图例或“EMA” | 跳转“设置 → 图表”，编辑 EMA 并保存 |

EMA 默认 20／60，周期范围 1—1000，所有币种共用。计算使用全部已加载收盘价，平移和缩放不重置计算起点；不足五倍周期时显示预热标记并尝试补充历史。继续获得更早历史后，EMA 初始种子可能得到修正。

左侧工具栏及顶部“绘图”菜单提供趋势线、水平线、射线、竖线、矩形、文字、斐波拉契回撤、测距。选择工具后在图内点击锚点；两点工具点击两次。完成一个对象后继续保留当前工具，可连续绘制；绘图模式下也可直接双击已有对象编辑属性，不会额外创建画线，关闭属性后仍保留当前工具。按 Esc 或点击光标工具退出绘制模式后，可选择和拖动已有对象。选中对象后拖动整体或锚点，双击／右键编辑属性；绘图对象按类型自动命名为“趋势线1、趋势线2”“水平线1”等，同币种各周期共用编号；旧画线会自动补名，新对象使用当前未占用的最小正整数编号；删除或重命名释放的编号可复用，全部删除后从 1 开始，已有对象不会重新编号。双击对象或在对象窗口选择“属性”，可修改“名称”（最多 80 字，不能为空），保存后在对象列表显示并随画线持久化；重命名支持撤销，锁定对象也可改名而不改变坐标。“对象”窗口可管理屏幕外和重叠对象，支持 Ctrl 点选、Shift 连选、“全选”按钮及 Ctrl+A 全选。点击“删除所选”或在列表按 Delete 可批量删除，锁定对象会保留并提示；关闭窗口后在图表按 Ctrl+Z 可一次恢复整批。显隐和锁定／解锁也作用于全部选中项，属性编辑限单选。锁定后须先解锁再修改或删除。工具栏较短时可滚动，全部工具始终可从“绘图”菜单选择。

**K 线磁吸默认开启**：通过左侧磁铁按钮或顶部绘图菜单开关。绘制起点、终点或拖动单个锚点时，鼠标靠近可见 K 线最高价／最低价 14 个逻辑像素以内，自动吸附到最近的高低点，保存该根 K 线的时间和对应价格。绿色圆圈与“磁吸 · 最高／最低”标签标出实际落点。按住 Alt 可临时自由定位，松开恢复。开关在停靠、浮动和重启后保留。整体移动对象保持原有形状；数据缺口、成交量区域和价格视口外的点不参与吸附。

斐波拉契默认比例为 0、0.236、0.382、0.5、0.618、0.786、1，比例、标签和各级颜色均可编辑。紧凑视图下自动减少重叠文字，比例线仍显示，放大后可查看完整标签。测距显示价格差、百分比、时间和周期根数。Esc 取消绘制，Delete 删除选中对象，Ctrl+Z 撤销，Ctrl+Shift+Z／Ctrl+Y 重做；这些快捷键只在图表有焦点时生效。

绘图按环境、币种保存，各周期共享，也可设置单个对象的显示周期。坐标是时间与价格，缩放、补充历史和切换周期不会改写锚点；重启后恢复。视口按环境、币种和周期保存，浮动和停靠使用同一图表实例。撤销历史保留当前运行期间最近 100 次修改。

### 区域填色与价位快捷操作

趋势线、水平线、竖线、射线或矩形实际相交形成封闭区域后，在区域内部右键选择“封闭区域填色…”。未闭合区域的菜单项不可用，屏幕边缘不作为封闭边界。颜色保存为独立区域对象，默认不透明度 20%；双击区域或右键“编辑属性”可调整填充颜色、不透明度和边框。矩形同样支持自定义填充，不透明度设为 0 可取消填色。

填色区域保存时间与价格坐标，随图表平移缩放，并支持对象管理、锁定、隐藏、删除、撤销及重启恢复。原边界线仍是独立对象；之后移动边界线不会自动重塑已有填色区域，可修改区域锚点或删除后重新填色。

主图右键“复制价格”复制点击位置的价格，按合约价格步长取整，不含千分位分隔符；光标模式下双击主图空白区域也会复制价格。双击绘图对象仍编辑属性，双击价格轴／时间轴仍保留原恢复操作。

右键“提醒…”直接在工作台弹出价格提醒窗口，自动填入合约与价位，保存或取消后仍停留在盘面，不跳转设置页；盘面浮动时弹窗归属该浮动窗口。现价之下默认向下提醒、现价之上默认向上提醒，可自行修改。已有提醒仍可在“设置 → 提醒规则”统一管理，继续遵循启动基线、行情新鲜度和冷却规则。

右键“快捷下单 → 限价开多／限价开空”将点击价位带入当前环境的下单表单，填写张数后通过“检查并确认下单”完成原有校验与人工确认。该操作本身不提交订单，不绕过真实交易解锁条件；环境已切换时旧菜单不带入委托或提醒。

交易图上的持仓均价、挂单和止盈止损触发线为只读，标注方向、张数和触发价格性质；账户过期时同步标灰。拖动分析画线不会提交或修改订单，交易继续通过右侧面板校验并人工确认。

在“设置 → 提醒规则”配置：

- 最新价格、窗口涨跌幅、放量、持仓未实现盈亏和收益率；支持同币种全部满足／任一满足，最多 8 项条件。
- 放量基于上一根已收盘 K 线，比较此前 20 根均量；涨跌幅需要实际积累完整观察窗口，启动后不会用缺失数据补造变化。
- 订单事件独立订阅：全部成交、部分成交、已撤销、失败。
- 冷却时间默认 300 秒。启动、修改规则、断线或休眠恢复后先建立基线；条件持续成立不重复刷屏，要先恢复再重新满足。
- 报价超过 30 秒、账户超过 20 秒或图表失去新鲜度时不据此触发数值提醒。工作台检查 OKX 报价时间戳，拒绝旧的缓冲消息。

规则先记录事实并提醒，AI 分析异步补充。通知可暂停，事件仍保留；声音默认关闭。AI 自动解读只针对触发事件，串行处理，不扫描全市场。电脑休眠、关机或程序退出期间不提供监控。

## 账户和交易

### 本地模拟盘（无需 API）

在“设置 → OKX 账户”将环境选择为“本地模拟（无需 API）”，保存并连接。尚未配置 OKX 模拟账户时，也可点击下单面板“开启本地模拟（免 API）”。已有环境不会在升级时自动切换。

本地模拟初始虚拟资金为 **100,000 USDT**，每笔成交收取成交额 **0.05%** 的虚拟手续费。使用正常下单表单及人工确认流程，支持市价／限价、开多／开空、部分／全部平仓、撤单、附带及独立止盈止损、杠杆调整。持仓采用单向模式；调整某合约与保证金模式的杠杆前，须先平仓并撤销对应挂单。

市价按收到的最新有效 OKX 报价全部成交；限价达到条件后按该报价全部成交，止盈止损也按最新价触发。开仓挂单冻结保证金及预估手续费，平仓挂单占用可平张数。资金、持仓、挂单、保护单和成交写入本地数据库，重启后恢复，与 OKX 模拟及真实账户隔离。本地操作不读取 OKX 凭据，不向交易所发送交易请求，也不计入真实交易解锁验收。

公开行情和合约规格仍需联网加载，超过 30 秒的旧报价不成交。此模拟采用简化模型，不模拟盘口排队、部分成交、滑点、资金费率和强平；全仓／逐仓分别记录仓位和杠杆，但不模拟交易所的逐仓风险隔离。退出、离线或休眠期间不补造历史成交，恢复后按新收到的有效报价继续判断挂单。

### OKX 账户

使用交易所账户时，在“设置 → OKX 账户”选择 OKX 模拟交易或真实环境，填写该环境对应的 API Key、Secret 和 Passphrase。只需读取与交易权限，不需要提现权限。留空保存可切换到此前存储的凭据。

交易只支持 OKX USDT 本位线性永续。支持市价／限价、开多／开空、部分／全部平仓、撤单，以及附带或独立设置止盈止损。数量单位是**合约张数**；下单面板显示每张面值、最小张数、数量与价格精度。账户持仓模式由交易所读取，不在本软件切换。

下单流程为填写或载入 AI 草稿 → 参数与数据检查 → 显示环境、方向、数量、价格、实际杠杆与退出条件 → 人工确认 → 提交。AI 没有直接调用交易接口的能力。选择持仓后可载入全部可平张数，再编辑为部分平仓。

杠杆查询与变更是独立操作，变更同样需要确认。订单与止盈止损结果分别显示：主订单接受不代表附带保护已生成，必须以交易所实际订单状态为准。独立设置保护时不会自动取消已有保护单；可在“止盈止损”表中显式撤销。

提交前先写入本地记录并生成唯一客户端订单编号。遇到超时或未知响应时保留“结果待确认”，按原编号查询，重启后继续对账；不会自动重发。该合约存在未知提交时，阻止再次提交新单。撤单、设置保护、杠杆调整同样记录状态，超时后查询实际结果，在核实前阻止重复操作。

### 模拟环境验收与真实交易解锁

真实交易默认不可提交。设置页显示以下 OKX 模拟环境证据（本地模拟盘不计入）：本软件发起的开仓成交、平仓成交、撤单完成、交易所已生成同时包含止盈与止损的保护单，以及断线后账户／订单重新同步。完成后才能在真实环境提交；这不是自动执行测试交易的按钮，每个模拟操作也由用户确认。

自动化测试使用本地 HTTP 服务及模拟响应，不接触真实资金。未配置用户模拟账户前，不能把本地测试视作该账户在 OKX 的实际验收。

## AI 服务、提示词与草稿

支持 Responses、Messages 和 Chat Completions。填写服务名称、协议、接口地址和模型；地址可以是服务根地址、版本前缀或对应完整端点。云端服务要求 HTTPS，本机模型可用 localhost / 127.0.0.1 的 HTTP。密钥保存后不再回填到界面。

保存服务后可“测试已保存连接”，再为事件解读、下单前分析、复盘各自选择服务及默认模板。不同场景可使用不同模型。协议兼容指对应的基础文本请求／响应，不包含图片、工具调用或特定厂商私有参数。

模板支持新建、复制、编辑、保存新版本和恢复默认内容。变量包括 `{{context}}`、`{{market}}`、`{{positions}}`、`{{trades}}`、`{{events}}`、`{{question}}`；“预览发送内容”可查看实际上下文。未包含 `{{context}}` 时仍附加事实资料，防止分析缺少基础数据。单次请求可临时编辑提示词而不修改模板。

复盘页可以取消请求或重试，失败保留错误状态。原交易分析与订单草稿代码和配置保留，但新工作台暂不提供相关操作入口。模型不可用不影响行情、提醒、手工交易和记录。

## 复盘

同步整个连接账户的 USDT 永续成交，包括手机端、网页端和本软件产生的交易。首次默认近 30 天，支持自选时间范围，按接口分页。常用归档接口有回溯限制，因此最多向这些接口请求最近 89 天；更早的已保存本地记录仍可使用。页面显示每次同步请求范围与接口失败，不能把“分页完成”理解成历史起点前没有持仓。

按合约、保证金模式和方向归集开仓到归零的过程；包含加仓、部分平仓和净仓反转，反转拆分为平旧仓与开新仓。缺少开仓、订单保证金模式或盈亏字段时标记不完整。费用与已实现盈亏用 Decimal 计算；资金费用无法唯一归属、或费用不是 USDT 时单列，不假设汇率或归属。

选中单笔／多笔交易，或不选行而使用时间范围，再选择和编辑提示词生成。跨越范围边界的交易展示完整已知过程。只有本软件记录过的交易才具备当时的提醒、行情、分析和可选理由；其他交易明确标注过程信息缺失。

报告按版本追加保存数据快照、提示词、服务／模型与生成时间。重新生成不覆盖旧版。选择历史报告后填写问题，可继续追问并保存新的报告。历史报告不因后续修改模板或同步新数据而悄悄改变。

## 开发验证

```powershell
uv run --locked --group dev pytest -q
uv run --locked python tools/verify_workbench.py
uv run --locked python tools/verify_workbench.py --all
uv run --locked python tools/check_workbench.py
uv run --locked python tools/check_workbench.py --direct
.\tools\build_installer.ps1 -IsccPath "D:\Inno Setup 6\ISCC.exe"
uv run --locked python tools/smoke_test.py
```

`verify_workbench.py` 使用临时目录、虚构示例数据和空凭据生成工作台、复盘、设置、浮动面板、绘图及属性窗口截图，不读取用户账户；`--all` 检查 100%—200% 五档 DPI 和紧凑布局。`check_workbench.py` 只读检查 OKX 公开报价、历史 K 线和 K 线 WebSocket。原有 `tools/verify_ui.py --all` 继续验证迷你窗口和设置页 DPI。

安装包为 `dist/installer/CoinPilotAI-Setup-<版本>-x64.exe`，只为当前用户安装，无需管理员权限。目录版 `dist/coinpilot-ai/` 是中间产物，必须连同 `_internal/` 一起使用。安装后通过快捷方式启动；升级前从托盘退出。安装和卸载均保留用户数据；从旧单文件版迁移后，应在应用设置中重新保存已有开机启动设置。构建工具与隔离环境验收见 [README](README.md#Windows-安装包) 和 [验收清单](installer/VALIDATION.md)。

协议核对依据：[OKX V5 文档](https://www.okx.com/docs-v5/en/)、[Responses](https://developers.openai.com/api/reference/resources/responses/methods/create)、[Chat Completions](https://developers.openai.com/api/reference/resources/chat/subresources/completions/methods/create)、[Messages](https://platform.claude.com/docs/en/api/messages/create)。
